# clickops

Scripts (not Terraform) that build the estate's deliberately unmanaged resources — clicked or CLI'd into existence, never imported into any IaC state. Each script appends the IDs it creates to manifest.json so they can be torn down without drift.
