# baseline

Account telemetry Terraform (task P1-03): Config, CloudTrail, the CUR 2.0 export, Cost Optimization Hub and Compute Optimizer enrollment. Applied as condor-bootstrap, before any estate workload exists, so history starts accumulating early.
