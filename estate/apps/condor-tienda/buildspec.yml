version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.12
  build:
    commands:
      - pip install -r requirements.txt -r requirements-dev.txt
      - pytest
artifacts:
  files:
    - '**/*'
