# condor-harness

Shared platform tooling — GitHub Actions workflows only, no application
code. Never treated as an app in grouping logic (`CLAUDE.md` §9).

Workflows are built in task P1-14 (history and traffic generators). This
repo exists now, empty, so P1-14 has somewhere to push to and so P2-04's
GitHub App installation list (four app repos, never this one) is already
accurate.
