# P1-12 · Promo 2024 (clickops, isolated subnet)

| Field | Value |
|---|---|
| Phase | Phase 1 · Mount the estate to detect |
| Stream | I |
| Depends | P1-04 |
| Gate | `approve-apply` |
| Paths | estate/clickops/promo/ |

Before starting: read `CLAUDE.md`, confirm every dependency is `done` in `tasks/STATUS.md`, run the account guard for any AWS write.

**Goal.** A forgotten campaign that should be flagged for decommission.

**Build**, in the `promo` subnet of `condor-vpc` (ADR-044-network-consolidation — there is no second region), with all IDs appended to `manifest.json`:
- **EC2 `condor-promo-2024`**
  - Amazon Linux 2023, `t3.micro`, `condor-promo` subnet.
  - No instance profile, so it is not SSM-managed (PLANTED: AK-COV-04). No tags.
- **Unattached EBS volume**: 20 GiB gp3 (PLANTED: AK-INF-07).
- **Elastic IP**: allocated, not associated (PLANTED: AK-INF-07).
- **Dead pipeline**
  - Create CloudFormation stack `condor-promo-2024-web` with a trivial resource, such as an SSM parameter.
  - Create CodePipeline `condor-promo-2024`: source from a new S3 bucket `condor-promo-src-<acct>`; deploy action CloudFormation targeting that stack.
  - Run the pipeline once, then delete the stack (PLANTED: AK-PIP-08).

**Acceptance**
- [ ] `aws codepipeline get-pipeline --name condor-promo-2024` succeeds, and `aws cloudformation describe-stacks --stack-name condor-promo-2024-web` returns does-not-exist.
- [ ] The instance is running with no instance profile.

**Cost limit.** USD 0.05/hour.
