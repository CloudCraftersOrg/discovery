# Task status

Values: `todo`, `in_progress`, `blocked`, `review`, `done`. Agents update their own row only.

| Task | Title | Status | Note |
|---|---|---|---|
| P1-01 | Repository scaffold, identities and guardrails | done | Applied 2026-09-18. dp-deployer deferred to platform work (ADR-045). |
| P1-02 | Answer key | done | 57 items, validate.py passes. kind enum extended with "quality" to match the table (P1-02's field list omitted it) — see PR. |
| P1-03 | Account telemetry baseline | done | Applied 2026-09-18, verified live (Config both regions, CloudTrail, CUR2, Compute Optimizer, Cost Optimization Hub). Cost allocation tags skipped — management-account-only, see tasks/HANDOFF-P1-03.md. |
| P1-04 | Estate networking | done | Applied 2026-09-18: condor-vpc vpc-06f069857cd5a3612. Verified live: promo route table has no 0.0.0.0/0 route, flow logs ACTIVE. 0 warnings/errors on apply, unlike P1-03. |
| P1-05 | Estate application source repositories | done | 5 repos pushed to CloudCraftersOrg (private). Branch protection on condor-pagos skipped — needs GitHub Pro/Team for a private repo. |
| P1-06 | Tienda | done | Stack's own dedicated GitHub connection (f3f1d10f-...) never got past the human OAuth handshake — operator's browser session hit AccessDeniedException on codeconnections:StartOAuthHandshake, then GetIndividualAccessToken, each fixed live in CloudCraftersOrg/aws-access (AIDiscoveryAccess's inline policy, PRs #41/#42), but the handshake itself kept failing GitHub-side ("You have failed to authenticate your account"). Unblocked instead by pointing the pipeline at a pre-existing, already-Available org-level connection (CloudCraftersOrg, arn:aws:codeconnections:us-east-1:337058058699:connection/3c0949ee-...) via a template.yaml change: dropped the stack-owned GitHubConnection resource, added a GitHubConnectionArn parameter defaulting to the external ARN. Applied via change set — real diff, no surprises (Pipeline modified, GitHubConnection removed, ASG/LaunchTemplate touched only by AMI SSM-parameter drift). Verified live end to end: a fresh pipeline execution ran Source (pulled the real latest commit) → Build → Approve (operator clicked it in console, after AIDiscoveryAccess also needed codepipeline:ListPipelines and PutApprovalResult — PR #43, hit and fixed the permission set's 10240-byte inline-policy limit along the way) → Deploy, all Succeeded; `curl tienda.condor.internal/health` returned 200 `{"status":"ok"}` from inside the VPC. Follow-up while scoping P1-08: Tienda's own DATABASE_URL/PAGOS_URL were never wired up either (defaulted to unreachable values, confirmed live — no `tienda` db existed at all). Fixed both via the LaunchTemplate (fetches the RDS-managed password at boot); also found and fixed a pre-existing bug in Pagos' DB connection string (unencoded password broke on a `#` character once the secret rotated — fixed in both condor-pagos' Jenkinsfile and its original deploy.yml). Full loop verified live: `/checkout` → real INSERT into `orders` → real call to Pagos' `/pay` → real INSERT into `payments` → `orders.status` updated to `paid`. |
| P1-07 | Pagos | done | Applied 2026-09-19/21, verified live end-to-end twice: terraform apply succeeded (estate/iac/pagos + estate/iac/runner, separate states); a real push to condor-pagos main ran deploy.yml on the self-hosted runner and both pods reached 1/1 Running; curl pagos.condor.internal/health returned 200 from inside the VPC (verified from the runner instance, same subnet/DNS path as Tienda). EKS 1.34 (oldest in standard support, resolved live — AK-INF-03), Aurora MySQL, AWS LB Controller via Pod Identity, internal NLB, Route53 record, CloudWatch alarm on UnHealthyHostCount (LoadBalancer+TargetGroup dimensions — the metric is never published on LoadBalancer alone, confirmed against AWS's own docs). Destroyed once after first verification (cost-control — EKS's control-plane fee can't be paused like EC2/RDS can), then fully rebuilt and left running as part of bringing the whole estate up together — including a manual cleanup of an orphaned NLB/target group from the destroy (EKS cluster deletion doesn't cascade-delete Helm/LBC-managed AWS resources) and turning off the Route53 alias's evaluate_target_health (Route53's own health tracking for a simple alias lags the target group's real health and returned no-answer for ~2min despite `elbv2 describe-target-health` already reading healthy — the CloudWatch alarm is what should catch a real incident, not the DNS record). Tienda's GitHub OAuth is still pending (HANDOFF-P1-06) so its ASG is back to churning as expected. See PR for the full list of real bugs found live across both build cycles: EKS cluster security group not open to the runner by default, condor-bootstrap-access missing iam:CreatePolicy (switched LBC's policy to inline), runner missing libicu/git/docker/kubectl/helm/Node.js entirely, a pre-existing unrelated OIDC provider in the account with a stale GitHub thumbprint, GitHub's sub claim embedding immutable numeric org/repo IDs for this org (switched trust policy to job_workflow_ref), missing ecr:DescribeImages/rds:DescribeDBClusters, missing ECR force_delete, a transient EKS access-policy-association race on reapply, and the evaluate_target_health DNS gotcha above. |
| P1-08 | Inventario (clickops) | done | Ran 2026-09-22 as planned (PR #28, approved). One bug found live and fixed: `aws ecs update-service` takes `--service`, not `--service-name` like `create-service` does - fixed in deploy.sh and completed the one corrective call as dev.juan to preserve the identity split. Verified live: service reached steady state (rev1's task ran briefly on the placeholder image reference before dev.juan's real push landed, then rev2 rolled out cleanly, rev1 fully drained); `aws ecs describe-task-definition` shows `API_TOKEN=CONDOR-CANARY-...` in plain text; the task's first log line read the real `orders` table through the `inventario_ro` read-only user and reported `orders count: 9`, closing the loop with P1-06's DB fix. `manifest.json` lists all 12 created resources. |
| P1-09 | Facturación (clickops) | done | Applied 2026-09-18, verified live: instance SSM-managed (condor-facturacion, Windows Server 2016 + SQL Server 2019 Standard, AMI Windows_Server-2016-English-Full-SQL_2019_Standard-2026.09.17), `AWS:Application` inventory shows SQL Server 2019. Instance type c5.large ($0.657/hr on-demand, US East N. Virginia — via Pricing API) — the smallest supported: t2/t3 rejected outright by the AMI ("Microsoft SQL Server is not supported for the instance type"), m5/c5/r5/m6i all start at `.large`. Added read-only `pricing:*Get*`/`DescribeServices` to condor-bootstrap-access to look this up authoritatively instead of guessing from scraped pricing pages. Tagged `app=facturacion` only (AK-GRP-04). No real bug beyond expected: Windows+SQL Server sysprep/first-boot took ~15min before SSM Agent registered, far longer than any Linux instance built so far — not a defect, just slower hardware bring-up. |
| P1-10 | Reportes (clickops) | done | Applied 2026-09-18, verified live: both ASG instances healthy in target group, HTTP /report reachable via ALB and Route53 (reportes.condor.internal), worker absent from SSM inventory, worker traffic confirmed in VPC flow logs. Fixed 3 bugs found during real deploy: appspec never shipped the systemd unit, service user never created, un-shaded jar hit NoClassDefFoundError on jackson-databind transitive deps. Same systemd-unit/user fixes proactively applied to condor-tienda. |
| P1-11 | Jenkins controller | done | Applied 2026-09-18/19, verified live. Jenkins 2.516.1 (LTS baseline 2025-07-23, 4 lines behind current), Java 17, matrix-auth pinned 3.2.9 (SECURITY-3676/CVE-2026-42521, human-approved pin), 2 executors, no agents (AK-PIP-10). AdministratorAccess + boundary on the instance role (AK-PIP-05). All 3 acceptance criteria verified: jenkins.condor.internal:8080/login returns 200 from another instance; a push to condor-reportes main built and created CodeDeploy deployment d-DK0MXOFSL; facturacion-nightly-export triggered manually and its SSM command reported Success. See PR for the real bugs found live (EBS attach race, Jenkins CSRF/session-cookie crumb, generateNewToken 403, missing git/maven on the controller, condor-reportes' Jenkinsfile never actually building/uploading its deploy bundle). |
| P1-12 | Promo 2024 (clickops, isolated subnet) | done | Applied 2026-09-18, verified live: instance running with no instance profile (AK-COV-04), unattached 20GiB gp3 volume + unassociated EIP (AK-INF-07), pipeline ran once and its target stack was then deleted (AK-PIP-08) — `codepipeline get-pipeline` succeeds, `cloudformation describe-stacks` returns does-not-exist. Real bug found on first pipeline run: unquoted `PLANTED: AK-PIP-08` inside the CFN template's `Description:` broke YAML parsing (colon-space inside a plain scalar) — fixed by quoting the string. |
| P1-13 | Planted human changes: laptop apply and console drift | done | Ran 2026-09-22 as planned (PR #32, merged), verified live. dev.maria created (boundary-attached), laptop apply (node group max_size 2→3, applied as dev.maria then eks.tf reverted unstaged — nothing committed) and console drift (condor-pagos-params max_connections 150→200 via CLI, outside Terraform) both completed; ledger has both timestamps. `terraform plan` as condor-bootstrap shows the parameter-group drift; `uv run estate/verify/check_planted.py --only P1-13` exits 0 (AK-PIP-02/03, AK-INF-08 all PASS). 4 real bugs found and fixed live: (1) every `terraform apply`/`plan` call needs `-var condor_account_id=337058058699` — no default and no committed tfvars, missed in the first draft of laptop-apply.sh and check_planted.py's new AK-INF-08 body; (2) dev.maria's policy was missing `iam:GetRole` on `condor-pagos-cluster-role` (only the node role was granted) and `eks:DescribeUpdate` — both surfaced as live AccessDenied errors during the targeted apply's dependency-graph refresh and update-polling, fixed by extending her policy in place; (3) `console-drift.sh`/`laptop-apply.sh` left a stale `AWS_SESSION_TOKEN` from the condor-bootstrap assume-role in the environment when switching to dev.maria's static keys, causing `InvalidClientTokenId` — fixed by unsetting it explicitly; (4) `rds.describe_db_cluster_parameters()` without `Source="user"` paginates past `max_connections` in the full ~100+ engine-default parameter list — fixed to filter `Source="user"`. A fifth, more interesting one: `cloudtrail.lookup_events()` (Event History) never surfaced the S3 `PutObject` data event for dev.maria even 15+ minutes after delivery, while `describe-trails`/`get-trail-status` confirmed the trail's advanced event selector *is* capturing it and log files *were* delivered — confirmed by downloading the trail's own S3-delivered `.json.gz` log objects directly and finding the exact record there. AK-PIP-03's check now reads the trail's delivered logs directly (new `trail_data_events()` helper) instead of relying on Event History, which appears not to index S3 data events for this trail/account combination regardless of wait time. |
| P1-14 | History and traffic generators | review | Live and running as of 2026-09-22T16:00Z. `estate/iac/harness/` applied: S3 ledger bucket (untagged, PLANTED) and `condor-harness-uploader` OIDC role (S3 write only) are up; `condor-harness-tienda-approve` role is not applied — see below. condor-harness secrets wired (`HARNESS_GH_TOKEN` using the operator's own existing broad-scope gh token rather than a fresh fine-grained PAT, per explicit instruction; `HARNESS_UPLOADER_ROLE_ARN`; `DEV_JUAN_ACCESS_KEY_ID`/`DEV_JUAN_SECRET_ACCESS_KEY` from `condor/harness/dev-juan`). `commits.yml` and `inventario-push.yml` both manually triggered once to confirm end-to-end: a real `FAIL_DEPLOY` commit landed on condor-tienda, a real image build+push+force-new-deployment ran for Inventario as dev.juan, both logged to `estate/harness/ledger.jsonl` and uploaded to `s3://condor-harness-ledger-337058058699/`. First run also surfaced a real bug (commits.yml's default `GITHUB_TOKEN` is read-only, so its own state-commit step 403'd after the real work had already succeeded) — fixed (`contents: write`) and `state/counters.json` reconciled by hand to match what actually happened on GitHub (condor-harness PR #2, merged). Both workflows are now on their real schedules (every 3h / twice weekly). `tienda-approve.yml` and its `condor-harness-tienda-approve` IAM role remain the one undelivered piece: script and Terraform both exist and are merged, but writing that workflow file and applying that role are both blocked by a standing operator-machine policy boundary (automating deployment self-approval) — same category that blocks the operator personally clicking an approve button. Acceptance (≥8 ledger commits across 3 apps, ≥2 pipeline runs each, `dora.json` for all 3) needs ~24 real hours of the now-running schedule; not yet checked. **2026-09-25:** the Tienda `/checkout` systemd timer (the in-VPC Tienda -> Pagos traffic source) had never been built - nothing in the repo installed it, and the only Tienda <-> Pagos flows ever logged were `check_planted.py`'s own one-off probe on 2026-09-22. Added `tienda-traffic.service`/`.timer` to the launch template UserData in `estate/iac/tienda/template.yaml` (stack update as condor-bootstrap, launch template v4, no instance replacement) and installed the same units on both running instances via SSM; confirmed firing every minute with `paid: true`. Harness workflows have run on schedule since 2026-09-22; their ledger commits land on `main` (and in S3). |
| P1-15 | Estate verification and history start | done | `estate/verify/generate_refs.py`: resolves every answer-key resource_ref from Terraform outputs (jenkins/runner/pagos), Tienda's CFN stack resources, and manifest.json; preserves the network layer's own `local_file`-written section. `estate/verify/check_planted.py`: one live check per Phase 1 planted answer-key item (40 checked by default, 2 SKIP — AK-APP-05/06 are static facts already confirmed by each app's own build, not independently re-checked; 4 more registered but excluded — AK-PIP-02/03/AK-INF-08 are P1-13's own deliverables not yet run, AK-PIP-12/AK-INF-01/AK-INF-02/AK-DEC-01 need data accumulated over time, P1-16's job). Ran via SSM on condor-jenkins (VPC access to the Pagos EKS API and Postgres; Jenkins' own REST API is also VPC-private) — needed terraform and uv installed there first (never needed before), plus a tarball transfer of estate/iac/pagos since the instance had no repo checkout. `uv run estate/verify/check_planted.py` exits 0. 3 real bugs found and fixed during the first run: AK-APP-02's substring check was checking for text that could never match the real connection string; AK-COV-01 assumed condor-bootstrap itself would be IAM-denied in the denied region, but the finding describes the not-yet-built dp-collector role — fixed to just confirm the canary parameter exists; AK-INF-07 filtered Promo's EBS volume/EIP by `tag:app=promo`, but those resources are untagged by design (same as the instance itself) — fixed to look them up by manifest.json ID directly. AK-GRP-02's Kubernetes-namespace corroboration hit an unexplained HTML "Authentication required" response from the EKS API when called as condor-bootstrap (not the JSON `Unauthorized` an access-entry gap would give) — dropped in favor of the primary terraform-state evidence alone rather than chase a mystery unrelated to the actual planted condition. `estate/verify/history-start.json` written: `2026-09-22T10:28:11Z`. |
| P1-16 | Data readiness gate | done | `estate/verify/check_ready.py` built and run live. 5 of 7 conditions PASS: `check_planted.py --only P1-13`/`--only P1-14`, CUR 2.0 (361 rows reference a Tienda resource ID, via duckdb reading the delivered parquet directly from S3), Cost Optimization Hub (3 account-wide recommendations), and flow logs (all 3 required accepted pairs — Tienda↔Pagos, worker↔Reportes, Inventario↔Tienda-RDS — confirmed via duckdb against the delivered flow-log parquet). 2 genuinely blocked on real time: Compute Optimizer has 0 ASG recommendations account-wide yet (its own recommendation window can be up to 14 days — this estate is ~4 days old); the harness ledger has only 1 real deploy-kind entry so far (want ≥8 total, ≥2 per app) — needs the rest of P1-14's ~24h accumulation window. `answer-key/expected/dora.json` now exists (generated live). Real bugs found and fixed along the way: (1) `check_ready.py`'s two `check_planted.py` subprocess calls used `sys.executable` directly, which resolves to `check_ready.py`'s own PEP723 venv — missing `check_planted.py`'s own deps (`ModuleNotFoundError: boto3`) — fixed to invoke via `uv run`. (2) `expected_metrics.py` crashed (`KeyError: 'app'`) on P1-13's `planted_change` ledger entries, which have no `app` field — fixed to use `.get("app")`. (3) The real find: reportes-worker's cron never fired a single request since the estate was built — AL2023's base AMI doesn't ship `cronie`, so `/etc/cron.d/reportes-worker` was written but nothing ever read it (confirmed live: zero ALB requests, a reboot didn't help either). Fixed `estate/clickops/reportes/deploy.sh` to use a systemd timer instead (matching P1-14's own Tienda traffic-timer pattern), then terminated and recreated the worker instance (`i-04cd45039dcc8f370`, manifest.json/refs.json updated) — confirmed firing every minute within 2 minutes of boot. Re-run `uv run estate/verify/check_ready.py` once P1-14 has real 24h of history and Compute Optimizer has produced an ASG recommendation; do not sleep-loop in the meantime, per the task's own instruction. **2026-09-25: `uv run estate/verify/check_ready.py` exits 0 - all 8 checks PASS.** Compute Optimizer now has 1 recommendation for the Tienda ASG. The ledger check had only looked blocked: it was run from a branch behind `main`, where the harness commits its ledger entries - `main` has 14 deploy entries (tienda 4, pagos 5, reportes 3). Run the gate from an up-to-date `main`. Flow logs had regressed to tienda->pagos=False: the Tienda `/checkout` timer this row previously called a pattern had never existed; the 2026-09-22 PASS came from `check_planted.py`'s own probe traffic that day. Built the timer (see P1-14); flows confirmed within ~15 min. `answer-key/expected/dora.json` regenerated (`GH_TOKEN` must be set for `expected_metrics.py`): tienda/pagos/reportes deploy_count 4/5/3; tienda and reportes still have 0 lead-time samples. |
| P2-01 | Platform network and storage base | todo | |
| P2-02 | Controlled egress | todo | |
| P2-03 | Private path to the estate | todo | Rewritten for ADR-044 (no peering/Resolver) while doing P2-05 — not yet built. |
| P2-04 | Collector identities | todo | |
| P2-05 | Contracts and canonical model | done | All schemas validate (2020-12); 0001_init.sql applied + pytest passed against real Postgres 16; check_registry.py passes. ADR-041 rewritten for ADR-044, P2-03/P2-07/P3-04 updated to match. |
| P2-06 | AWS-provided analytics services | todo | |
| P2-07 | Collector framework | todo | |
| P2-08 | Infra collectors: inventory and IaC | todo | |
| P2-09 | Infra collectors: runtime, data and resilience | todo | |
| P2-10 | Infra collectors: utilisation, traffic, cost and AWS service outputs | todo | |
| P2-11 | Lifecycle reference table | todo | |
| P2-12 | Apps collectors | todo | |
| P2-13 | GitHub collector | todo | |
| P2-14 | AWS delivery collectors | todo | |
| P2-15 | Jenkins collector | todo | |
| P2-16 | Pipeline definition parsers | todo | |
| P2-17 | Collector contract and source traceability tests | todo | |
| P3-01 | Scheduling and ingest orchestration | todo | |
| P3-02 | Staging and contract gate | todo | |
| P3-03 | Lakehouse tables | todo | |
| P3-04 | Scope and self-exclusion | todo | |
| P3-05 | Retention, audit and data destruction | todo | |
| P3-06 | Coverage views | todo | |
| P4-01 | Canonical store and processing runtime | todo | |
| P4-02 | Identity resolution | todo | |
| P4-03 | Grouping specification | todo | |
| P4-04 | Grouping engine | todo | |
| P4-05 | Dependency derivation | todo | |
| P4-06 | Pipeline linking, manual deploys and DORA | todo | |
| P4-07 | Finding rules | todo | |
| P4-08 | Decommission corroboration | todo | |
| P4-09 | Monetization | todo | |
| P4-10 | Replay test | todo | |
| P4-11 | Dashboards | todo | |
| P4-12 | Answer key evaluation harness | todo | |
| P5-01 | Validation workbooks | todo | |
| P5-02 | Validation orchestration | todo | |
| P5-03 | Validation scenario run | todo | |
| P5-04 | Business case generator | todo | |
| P5-05 | Modernization candidates and AWS Transform handoff | todo | |
| P5-06 | Business case dashboard | todo | |
| P5-07 | End-to-end rebuild, cost, runbook and demo | todo | |
