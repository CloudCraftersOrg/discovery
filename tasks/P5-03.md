# P5-03 · Validation scenario run

| Field | Value |
|---|---|
| Phase | Phase 5 · Business case for AWS Transform |
| Stream | P |
| Depends | P5-02 |
| Gate | `review` |
| Paths | platform/validation/scenario/ |

Before starting: read `CLAUDE.md`, confirm every dependency is `done` in `tasks/STATUS.md`, run the account guard for any AWS write.

**Goal.** Execute the scripted owner responses that the answer key expects.

**Build.** Script `platform/validation/scenario/fill.py`, which simulates owners by filling returned workbooks programmatically through `openpyxl`, never by editing the hidden columns:
- **Reportes**: confirm the ASG instances and the ALB. For `reportes-worker` choose `Reassign`, target `facturacion`, comment "exports billing data" (AK-VAL-01).
- **Tienda, Pagos and Inventario**: confirm all rows. Fill the `Application` sheet with fictitious owners.
- **Facturación**: confirm the Windows instance. Set the owner to "Finance IT".
- **Unassigned workbook**
  - Leave Promo rows blank on the first cycle.
  - Run the escalation with `sla_days=0` for the scenario.
  - On the second cycle the sponsor sets `Decommission OK` (AK-VAL-02).

The script uploads to the inbox in that order and waits for each parse by polling the ledger with a timeout. That wait is event-bound, not a sleep loop.

**Acceptance**
- [ ] Processed checks for AK-VAL-01 and AK-VAL-02 pass:
  - The worker is a member of Facturación with source `owner_validation`.
  - Facturación's baseline cost increased by the worker's cost.
  - Promo shows `escalated`, then `signed_off`, and the staged plan is attached.
- [ ] Before uploading the first workbook, the script saves a mart snapshot with `answer-key/evaluate/run.py --save-snapshot pre-validation`.
- [ ] `make evaluate PHASE=4` still passes after the grouping re-run. AK-GRP-06 is evaluated against the `pre-validation` snapshot; AK-VAL-01 against current state.
