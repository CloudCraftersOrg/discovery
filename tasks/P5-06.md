# P5-06 · Business case dashboard

| Field | Value |
|---|---|
| Phase | Phase 5 · Business case for AWS Transform |
| Stream | P |
| Depends | P5-04, P4-11 |
| Gate | `human-action` |
| Paths | platform/quick/ |

Before starting: read `CLAUDE.md`, confirm every dependency is `done` in `tasks/STATUS.md`, run the account guard for any AWS write.

**Goal.** The business case, visible in the same place as the other dashboards.

**Build.** Dashboard 7, **Business case**, added to `platform/quick/`:
- Baseline versus do-nothing, as a line chart.
- Avoidance by category, with bands.
- A realization schedule chart showing the three curves.
- Risk of inaction table.
- Candidates table, with patterns and evidence links.
- A visible `Target run-rate: pending AWS Transform` indicator.

**Acceptance**
- [ ] `deploy.py` is idempotent with seven dashboards.
- [ ] A human confirms the render. Values match `business_case.json` for three spot-checked cells, listed in the PR.
